spring:
  cloud:
    config:
      enabled: false
  application:
    name: transaction-service
  datasource:
    url: jdbc:mysql://ebank.cfuskkuqotmo.us-east-1.rds.amazonaws.com/transactions-service
    username: admin
    password: 123ayman
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always
